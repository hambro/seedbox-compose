
#STARTplexrequest#
plexrequests-%USER%:
  restart: always
  image: linuxserver/plexrequests
  container_name: seedbox-compose-plexrequests-%USER%
  environment:
    - PGID=%GID%
    - PUID=%UID%
  ports:
    - %PORT%:3000
  volumes:
    - /etc/localtime:/etc/localtime:ro
    - %BASE_HOME%/%USER%/dockers/plexrequests/config:/config
#ENDplexrequest#
